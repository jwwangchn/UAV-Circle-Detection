# aux_source_directory(. DIR_SOURCE_SRCS)
# add_library(MST_LIB ${DIR_SOURCE_SRCS})

# set(SOURCE_FILES MST.cpp Edge.h DenseGraph.h SparseGraph.h ReadGraph.h MinHeap.h LazyPrimMST.h)
# add_executable(03_Lazy_Prim ${SOURCE_FILES})
